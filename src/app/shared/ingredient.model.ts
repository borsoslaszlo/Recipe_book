export class Ingredient {

    // automatically crates the fields of class and assignes the parameters to this
    // class  .  Shorcut of explicit field an constructor definition 
    constructor (public name: string, public amount: number)
    {
    }
}